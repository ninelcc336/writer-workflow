# sync-state

状态：可执行流程说明，已最小脚本化

执行入口：

- 当当前章正文已通过人审、并已锁定正式版本后运行
- 详细规则见 `docs/sync-state.md`

## 步骤清单

1. 锁定当前章正式版本
2. 提取本章长期有效变化
3. 与旧 state 对照
4. 更新 `book/state/*.yaml`
5. 生成状态差异报告
6. 更新 `chapter_index.yaml`

## 最小输入

- 当前章正式版本
- 当前章 `plan.md`
- 当前章 `draft review` 报告
- `book/state/*.yaml`

## 推荐输出

- 更新后的 `book/state/*.yaml`
- `book/artifacts/reports/chapter-XXX-state-diff.md`

## 强约束

- 不得在正文未通过人审前运行
- 不得反向改正文
- 不得跳过状态差异报告
