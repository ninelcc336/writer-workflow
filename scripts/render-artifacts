# render-artifacts

状态：可执行流程说明，尚未脚本化

执行入口：

- 当当前章已完成 `sync-state` 后运行
- 详细规则见 `docs/render-artifacts.md`

## 步骤清单

1. 读取最新 `book/state/*.yaml`
2. 读取当前章 `state diff`
3. 重建剧情回顾类 artifacts
4. 重建状态总览类 artifacts
5. 写入 `book/artifacts/recaps/`

## 最小输入

- `book/state/*.yaml`
- 当前章 `state diff`
- 当前章正式版本

## 推荐输出

- `book/artifacts/recaps/plot-recap.md`
- `book/artifacts/recaps/state-recap.md`

## 强约束

- 不得反向修改 state
- 不得反向修改正文
- 不得把 artifacts 当成唯一事实源
